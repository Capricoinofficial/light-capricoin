# -*- mode: python -*-
a = Analysis(['C:/light/light'],
             pathex=['Z:\\home\\mmitech\\light-capricoin\\contrib\\build-wine'],
             hiddenimports=[],
             hookspath=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          name=os.path.join('dist', 'light.exe'),
          debug=False,
          strip=None,
          upx=True,
          console=False )
app = BUNDLE(exe,
             name=os.path.join('dist', 'light.exe.app'))
